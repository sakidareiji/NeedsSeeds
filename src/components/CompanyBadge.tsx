/** 企業アカウント(role='company')の投稿・プロフィールに付けるバッジ。 */
export function CompanyBadge() {
  return (
    <span className="rounded-full bg-sky-50 px-2 py-0.5 text-xs font-medium text-sky-700">
      企業
    </span>
  );
}
