# Needs Seeds MVP 要件定義・実装仕様書

- バージョン: 2.0(MVP / フェーズ1・確定版)
- 想定実装者: Claude Opus 4.8(AIコーディングエージェント)
- 発注者: 個人開発者(エンジニア経験あり、副業運用)
- 改訂履歴: v1.0 初版(マッチング広告・非金銭インセンティブ設計)→ v1.x 品質設計(品質査定・追記促し・注目順ソート)、一般アドバイス生成と将来のWeb検索拡張を追加 → v2.0 貢献スコア制(換金不可のreputation型)を導入し確定

---

## 1. プロダクト概要

### 1.1 一言で
日常や業務の「困りごと」を投稿すると、AIが解析して解決策(アフィリエイト案件・一般アドバイス)を自動提示し、良質な投稿にはAI査定による**貢献スコア**(換金不可)が蓄積されるWebサービス。

### 1.2 MVPのゴール
「投稿 → AI判定 → 解決策提示」のパイプライン1本を成立させ、アフィリエイト経由の初期収益とデータ(投稿+共感+解決報告+品質査定)の蓄積を開始する。

### 1.2.1 核となる思想(実装判断に迷ったらここに立ち返ること)
困りごとの投稿は無償の愚痴ではなく、価値ある貢献である。ただしその価値は現金ではなく、①露出(注目順での優先表示)、②信頼の可視化(貢献スコアとグレード)、③将来の発言力・間接実利、という形で報われる。報酬の主軸はあくまで「露出」と「解決策が返ってくる体験」であり、スコアはその足跡の可視化である(Stack Overflowのreputation / Redditのkarmaと同型の設計)。

### 1.3 ビジネスモデル(MVP時点)
- ASP(A8.net等)経由のアフィリエイト報酬。解決策提示の中に成果報酬型リンクを含める
- 企業向けデータ販売・直販広告枠はフェーズ3(本仕様のスコープ外)。ただしデータ設計はそれを見越して行う

### 1.4 非ゴール(MVPで作らないもの)
- **現金・ギフト券交換可能なポイント制度(あらゆる形態で禁止)**
  - 貢献スコア(F6)は実装するが、**換金・交換機能は一切持たせない**。「わかる」やスコアの閾値達成による金銭報酬も実装しないこと。理由: (a) 金銭報酬化した瞬間にスコアと「わかる」が不正の攻撃対象になる(相互わかる・複垢・生成AIによる投稿量産)、(b) 金銭が絡んだシグナルはニーズ規模の指標としてのデータ価値(フェーズ3の事業資産)を毀損する、(c) 外的報酬が共感ベースの投稿動機を破壊する(アンダーマイニング効果)、(d) 資金決済法・景表法・税務対応の法務コストと換金インフラ・不正監視の運用負担が発生する
  - 将来オプション(実装不要・設計判断の記録): ①貢献スコア上位者への権限開放(カテゴリタグ修正権、新規カテゴリ提案権などコミュニティ運営への参加資格)、②週次殿堂入り(高スコア投稿のトップページ掲出)、③企業閲覧通知「あなたの声が企業に届きました」(フェーズ3)、④期間限定の抽選・ガチャ形式キャンペーン(閾値保証型と異なり攻略のROIが成立しない。ただし常設禁止・ガチャ権はAI査定済みの標準品質以上の投稿のみ・景品は低額・確率明示・実施前に景表法の懸賞規制の専門家確認が条件)、⑤**間接実利への接続**: 案件マッチング企業への貢献スコア上位者の紹介提携など、スコアがキャリア・収入に間接的に繋がる導線(Stack Overflow Jobsが評判値を採用市場に接続したのと同型)。恒常的な換金・閾値報酬制度は将来も導入しない方針
- バーコード読み取りによる商品フィードバック
- 企業向けダッシュボード / データエクスポート
- ネイティブアプリ(Web/レスポンシブのみ)
- ユーザー同士のDM・フォロー機能
- 総合ランキング・レベル制(フェーズ2)

---

## 2. ターゲットと初期カテゴリ

- 初期メインカテゴリ: **フリーランス・個人事業主の困りごと**(確定申告、請求、ツール、営業、時間管理など)
- カテゴリはDB管理とし、運営が管理画面から追加・編集できること
- 初期カテゴリ案: `お金・確定申告` `仕事のツール` `営業・集客` `契約・法務` `働き方・時間` `暮らし・その他`

---

## 3. 技術スタック(指定)

| 項目 | 指定 | 理由 |
|---|---|---|
| フレームワーク | Next.js(App Router, TypeScript) | 投稿ページのSSR/SSGによるSEO(フェーズ2の柱)を最初から担保 |
| DB / 認証 | Supabase(PostgreSQL, Auth, RLS) | 低コスト・個人運用向き |
| LLM | Anthropic API(claude-sonnet-4-6) | 投稿の分類・判定・解決策マッチング |
| ホスティング | Vercel | 無料枠から開始 |
| スタイリング | Tailwind CSS | 実装速度 |
| メール | Resend等(認証メールのみ) | 最小限 |

制約:
- 月間ランニングコスト目標: 5,000円以下(初期)
- LLM呼び出しは非同期(投稿の保存をブロックしない)。キューはSupabase Edge Functions + DBフラグ、またはVercel の after/Queue 相当のシンプルな仕組みでよい。過剰なインフラ(専用ジョブワーカー、Redis等)は導入しない

---

## 4. 機能要件

### F1. ユーザー登録・認証
- メールアドレス+パスワード、または Google OAuth
- 登録時に**表示名(ニックネーム)**を設定。実名不要。投稿はすべて表示名で公開される
- プロフィールページ: 表示名、自己紹介(任意)、実績(F6のバッジ・カウント)を表示
- 未ログインでも投稿の閲覧は可能。投稿・リアクションはログイン必須

### F2. 困りごと投稿
入力項目(最小限に絞る):
- タイトル(必須、60字以内)
- 本文(必須、2,000字以内。「いつ・どんな状況で・何に困るか」のプレースホルダで誘導)
- カテゴリ(必須、単一選択)
- 困る度合い(必須、1〜5の5段階)
- 頻度(任意: 毎日 / 週数回 / 月数回 / たまに)

仕様:
- 投稿は即時公開(pending状態を挟まない)。ただしF7のAIモデレーションが非同期で走り、NG判定時は自動非公開+運営通知
- 投稿者は自分の投稿を編集・削除できる
- 論理削除とする(データ資産保全のため物理削除しない。ユーザー向けには「削除」と表示)

### F3. AI解析パイプライン(本プロダクトの核)
投稿保存後、非同期で以下を1回のLLM呼び出し(structured output / JSONモード)で実行する:

1. **カテゴリ検証**: ユーザー選択カテゴリの妥当性確認と、サブタグ(自由生成、最大5個)の付与
2. **商用ニーズ判定**: 投稿が「サービス・商品で解決可能な悩みか」を判定し、解決策タイプを分類(例: `会計ソフト` `見積もりサービス` `講座・学習` `ツール` `該当なし`)
3. **モデレーション判定**(F7と共通): 誹謗中傷 / 個人情報 / センシティブ(健康・借金・法律・アダルト)/ スパム のフラグ
4. **解決策マッチング**: F4の解決策マスタ(DBに登録された案件リスト)をコンテキストとして渡し、マッチする解決策ID(0〜3件)と、投稿内容に即した提示文(なぜこれが役立つかの1〜2文)を生成
5. **一般アドバイス生成(マスタにマッチしない場合)**: マッチする解決策が0件のとき、モデル知識に基づく一般的な対処ヒント(2〜3文、テキストのみ・外部リンクなし)を生成する。制度名や選択肢の紹介に留め、断定・診断をしないようプロンプトで制約すること。センシティブフラグ(F7)が立った投稿には生成しない。全投稿に対して「投稿すると何かが返ってくる」核体験を成立させるための機能である
6. **品質査定と貢献スコア算出**: 投稿の具体性(状況・頻度・文脈が読み取れるか)・有用性を0〜100の品質スコアで採点する。品質スコアは表示ソート(F11)の係数として内部利用し、**ユーザーには表示しない**(可視化は攻略対象になるため)。同時に、品質スコアの段階に応じた**貢献スコア**(例: 高品質+10 / 標準+3 / 低品質+1)を投稿者に付与する。モデレーションフラグ(誹謗中傷・スパム・害悪)が立った投稿の貢献スコアは0とする。段階と点数のマッピングは設定ファイルで調整可能にすること。ユーザーに見えるのは獲得した貢献スコアのみで、査定の内訳・基準は開示しない
7. **追記促し(聞き返し)**: 品質スコアが低い投稿に対し、責めないトーンの短い問いかけ(例:「どんな場面でそう感じることが多いですか?」)を1つ生成。投稿詳細ページに運営AIからのコメントとして表示し、投稿者は本文への追記で応答できる。品質を査定で弾くのではなく対話で育てる、本プロダクトの品質設計の中核機能である

出力はJSONでDBに保存。LLM出力のバリデーション(zod等)を必ず実装し、パース失敗時はリトライ(最大2回)→失敗時は「解析待ち」のまま人力確認キューへ。

**プロンプトは `/prompts` ディレクトリにファイルとして分離**し、コード変更なしで調整可能にすること。

### F4. 解決策マスタと自動提示
- 運営が管理画面から解決策(案件)を登録する: 名称、説明、対象カテゴリ/解決策タイプ、リンクURL、**アフィリエイトか否かのフラグ**、掲載状態
- F3のマッチング結果に基づき、投稿詳細ページに「解決のヒント」セクションとして表示
- **アフィリエイトリンクには必ず「PR」表記を付す**(景表法ステマ規制対応)。表記はコンポーネントレベルで強制し、フラグがtrueなら非表示にできない実装とする
- 非アフィリエイトの解決策(無料ツール、公的制度など)も同一UIで混在させる。「収益案件にだけ反応するbot」に見せないことが製品要件である
- マスタにマッチしない投稿には、F3-5の一般アドバイスを同じ「解決のヒント」セクションに表示する。この場合は「AIによる一般的な情報であり、専門的な助言ではありません」の注記を必ず添える(コンポーネントで強制)
- マッチも一般アドバイスもない投稿(センシティブ等)には何も表示しない(無理な提示をしない)
- クリックは計測する(F9)

将来拡張(実装不要・データ構造のみ対応):
- フェーズ2で、マスタにマッチしない投稿へのWeb検索による解決策提示(運営管理のドメイン許可リスト内に限定)と、AI提示解決策に解決報告が付いた際のマスタへの昇格フロー(管理画面で承認→ASP案件があればアフィリエイトリンクに差し替え)を予定している
- このため `post_solutions` に `source(master/ai_generated)` カラムを最初から持たせ、提示元を区別できる構造にしておくこと

### F5. リアクション: 「わかる」
- 各投稿に「わかる」ボタン(1ユーザー1投稿1回、取り消し可)
- 投稿一覧・詳細に「わかる」数を表示
- 投稿者への通知: 「わかる」が 1, 5, 10, 25, 50... の節目で通知(通知の連打防止)

### F6. 解決報告・貢献スコア・実績
解決報告:
- 投稿者は自分の投稿に対し「解決した」を報告できる(任意で「何で解決したか」を選択: 提示された解決策 / 自力 / その他)
- 提示解決策で解決した場合、どの解決策かを選択(教師データとして保存)
- 投稿に「解決済み」バッジが表示される
- 「わかる」を多く集めた投稿の解決報告、および他ユーザーの「私も解決した」報告には追加の貢献スコアを付与する(設定ファイルで調整可能)

貢献スコアとグレード:
- 貢献スコアはF3-6のAI査定+解決関連の加点で蓄積される、**換金・交換不可**の累積値
- スコア帯に応じたグレード(例: 芽 → 双葉 → 若木 → 大樹、名称は仮)をプロフィールと投稿者名の横に表示する。グレードの閾値・名称は設定ファイルで管理
- スコアは減らない(downvote的な減点はMVPでは実装しない。モデレーション違反は投稿の非公開化で対応する)
- 貢献スコアの使途(権限開放・殿堂入り等)はフェーズ2以降(1.4参照)。MVPでは蓄積と表示のみ

プロフィール実績表示: グレード、貢献スコア、投稿数、もらった「わかる」合計、解決済み数
通知: 自分の投稿が解決策提示を受けたとき / 貢献スコア獲得(投稿の査定完了時) / グレード昇格 / 解決の節目

### F7. モデレーション
- F3のパイプラインでNGフラグが立った投稿は自動で非公開化し、運営通知
- センシティブフラグ(健康・借金・法律)は**公開は維持するが商用マッチング(F4)を無効化**する
- ユーザーによる通報ボタン(理由選択式)。通報は運営キューへ
- 運営は管理画面から公開/非公開/削除を操作できる

### F8. 通知
- アプリ内通知のみ(ベルアイコン+一覧)。メール通知はMVPでは実装しない
- 種別: わかる節目 / 解決策が提示された / 貢献スコア獲得・グレード昇格 / 運営からのお知らせ

### F9. 計測・分析(最小限)
- イベントをDBの `events` テーブルに記録: 投稿作成、解決策表示(imp)、解決策クリック、わかる、解決報告
- 解決策クリックは `/go/[solutionLinkId]` のリダイレクトエンドポイント経由とし、クリックログを確実に取る
- 外部アナリティクスは Vercel Analytics 程度でよい

### F10. 運営管理画面
- 運営ロール(usersテーブルのroleカラム)のみアクセス可
- 機能: 投稿の一覧・検索・公開状態変更 / 解決策マスタCRUD / カテゴリCRUD / 通報キュー / 解析失敗キュー / 主要KPI(投稿数、DAU、クリック数)の簡易表示
- **種投稿の一括投入機能**: CSVまたはJSONで投稿をインポートできる(コールドスタート対策。作成者は運営が用意した複数のシードアカウントに割り当て可能)

### F11. 公開ページとSEO(フェーズ2の布石として最低限)
- **一覧のデフォルトソートは「注目順」とする**: 新着性 × 「わかる」数 × 品質スコア(F3-6)を係数とした加重スコア順。純粋な新着順は切り替えタブとして残す。UGCの体験品質は表示アルゴリズムで決まるため、低品質投稿を弾くのではなく目立たせない設計を採る。係数は設定ファイルで調整可能にすること
- 投稿詳細ページはSSRで、タイトル・本文からmeta/OGPを生成
- URL設計: `/posts/[id]-[slug]`(slugはタイトルから生成)
- カテゴリ別一覧ページ `/c/[category]`
- sitemap.xml 自動生成、robots.txt
- 非公開投稿は404

---

## 5. データモデル(概要)

実装者はこれを基にマイグレーションを作成すること。カラムの追加は裁量に任せるが、削除・統合は要相談。

```
users            id, display_name, bio, role(user/admin/seed),
                 contribution_score(int, 非正規化キャッシュ), created_at
categories       id, slug, name, sort_order, is_active
posts            id, user_id, category_id, title, body, severity(1-5),
                 frequency, status(published/hidden/deleted),
                 ai_status(pending/done/failed), resolved_at,
                 resolved_by(solution/self/other), resolved_solution_id,
                 empathy_count(非正規化キャッシュ), created_at, updated_at
post_analyses    id, post_id, sub_tags(jsonb), commercial_type,
                 moderation_flags(jsonb), is_sensitive(bool),
                 quality_score(int 0-100), follow_up_question(text?),
                 raw_llm_output(jsonb), model, created_at
solutions        id, name, description, url, is_affiliate(bool),
                 commercial_types(text[]), category_ids(int[]),
                 status(active/paused), created_at
post_solutions   id, post_id, solution_id?, source(master/ai_generated),
                 pitch_text(AIが生成した提示文/一般アドバイス本文),
                 rank, created_at
empathies        id, post_id, user_id, created_at  (unique: post_id+user_id)
contribution_logs id, user_id, post_id?, points(int), reason
                 (ai_assessment/resolution/etc), created_at
                 ※スコアの監査可能性と再計算のため、必ず履歴で持つ
notifications    id, user_id, type, payload(jsonb), read_at, created_at
reports          id, post_id, user_id, reason, status, created_at
events           id, type, user_id?, post_id?, solution_id?,
                 meta(jsonb), created_at
```

設計上の注意:
- 将来の企業向けデータ販売を見越し、`posts` と `post_analyses` は退会後も保持する(users.display_name の匿名化で対応)。退会処理は個人識別情報の匿名化とし、投稿の物理削除は行わない
- RLS: 自分のデータのみ書き込み可 / published投稿は全員読み取り可 / admin はサービスロールで操作

---

## 6. 画面一覧

1. トップ(新着投稿一覧+カテゴリタブ+サービス説明ヒーロー)
2. 投稿詳細(本文、わかるボタン、解決のヒント、解決報告UI)
3. 投稿作成・編集フォーム
4. カテゴリ別一覧
5. プロフィール(自分/他人)
6. 通知一覧
7. ログイン / 新規登録
8. 静的ページ: 利用規約、プライバシーポリシー、運営者情報(**内容はプレースホルダでよい。ただしページとルーティングは必ず用意する**)
9. 管理画面(F10)

デザイン方針: シンプル・清潔・モバイルファースト。「愚痴の掃き溜め」ではなく「困りごとが解決に向かう場所」のトーン。カラーは温かみのある1アクセントカラー+ニュートラル。

---

## 7. 非機能要件

- レスポンス: 投稿保存はLLM処理を待たずに完了(体感1秒以内)。解析結果は後から反映
- LLMコスト: 1投稿あたりの解析は1回のAPI呼び出しに収める設計とする
- セキュリティ: RLS徹底、入力サニタイズ、レートリミット(投稿: 10件/日/ユーザー、わかる: 200回/日)
- 可用性: 個人運用のため、LLM障害時もサイト閲覧・投稿は継続できること(解析だけ遅延)
- テスト: AI解析パイプラインのバリデーションとF4のPR表記強制については必ず自動テストを書く。その他はクリティカルパス(投稿作成、認証)の統合テスト程度でよい

---

## 8. マイルストーン(実装順序の指定)

1. **M1: 骨格** — 認証、投稿CRUD、カテゴリ、一覧・詳細表示(SSR)
2. **M2: 核** — AI解析パイプライン、解決策マスタ、自動提示、PR表記、クリック計測
3. **M3: 循環** — わかる、解決報告、貢献スコア・グレード、通知、プロフィール実績
4. **M4: 運営** — 管理画面、種投稿インポート、モデレーション、通報
5. **M5: 公開準備** — SEO(meta/OGP/sitemap)、静的ページ、レートリミット、デプロイ

各マイルストーン完了時に動作確認可能な状態でコミットすること。

---

## 9. 受け入れ基準(抜粋)

- [ ] 新規ユーザーが登録→投稿→(1分以内に)解決策が表示される一連の流れが動く
- [ ] マスタにマッチしない投稿にはAIの一般アドバイスが表示され、「専門的な助言ではない」注記が必ず付く(コンポーネントテストで担保)
- [ ] アフィリエイトフラグ付き解決策に必ずPR表記が出る(コンポーネントテストで担保)
- [ ] センシティブ判定の投稿には解決策・一般アドバイスのいずれも表示されない
- [ ] 「わかる」を押すと投稿者に通知が届く(節目のみ)
- [ ] 解決報告するとバッジが表示され、どの解決策で解決したかが記録される
- [ ] 投稿の査定完了時に貢献スコアが付与・通知され、プロフィールのスコアとグレードに反映される
- [ ] モデレーションフラグが立った投稿には貢献スコアが付与されない(0)
- [ ] 貢献スコアの換金・交換に類するUI・APIが存在しない
- [ ] 運営がCSVで種投稿50件を一括投入できる
- [ ] 未ログインで投稿詳細がSSRされ、OGPが正しく出る
- [ ] LLM APIを止めても投稿・閲覧が正常動作する

---

## 10. 実装者への申し送り

- **金銭的なポイント・報酬・換金に関する機能は、どんな形でも実装しないこと**(1.4参照)。貢献スコア(F6)は実装するが、換金・交換・購入に繋がるテーブル・カラム・UIの先回り実装は一切不要
- プロンプト、カテゴリ、解決策はすべて運用中に頻繁に変わる前提。ハードコードせずデータ/設定として外出しすること
- 迷ったら「機能を足す」より「削る」を選ぶこと。本仕様に書かれていない機能の追加実装は行わず、提案として報告すること
- 環境変数の一覧と、ローカル開発手順(Supabaseのセットアップ含む)をREADMEに必ず書くこと
