-- ============================================================================
-- 解決報告に「どうやって解決したか」の記述を追加(F6/F9)。
--
-- 「自力で解決」「その他」は構造化された解決策(resolved_solution_id)を持たない
-- ため、投稿者が解決方法を自由記述で残せるようにする。同じ困りごとを持つ他の
-- ユーザーの参考になり、F9 の成果データとしても活用できる。
-- ============================================================================

alter table public.posts
  add column resolution_note text
    check (resolution_note is null or char_length(resolution_note) <= 1000);
