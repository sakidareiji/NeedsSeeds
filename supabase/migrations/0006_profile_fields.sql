-- ============================================================================
-- プロフィール編集機能の拡張: 性別・年齢(任意入力)
--
-- 実名は不要という仕様(§F1)を踏襲し、いずれも任意項目とする。
-- 年齢は生年月日ではなく単純な整数(自己申告)とし、退会時の匿名化(§5)も
-- display_name のみで足りるため、これらの列は特別扱いしない。
-- ============================================================================

alter table public.users
  add column gender text check (gender in ('male', 'female', 'other', 'unspecified')),
  add column age smallint check (age between 0 and 150);
